version: '3.8'

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: ride_sharing_postgres
    environment:
      POSTGRES_DB: postgis_ride_sharing
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: bunz3120
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.db:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgis_ride_sharing"]
      interval: 30s
      timeout: 10s
      retries: 3

  app:
    build: .
    container_name: ride_sharing_app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:bunz3120@postgres:5432/postgis_ride_sharing
    volumes:
      - .:/app
    working_dir: /app

volumes:
  postgres_data: